# Action Plan

1. Replace noisy retrieval inputs with `primary_retrieval_text`.
2. Rebuild embeddings from official legal text first.
3. Use `search.py` for intent-aware ranking.
4. Apply negative penalties for unrelated governance families.
5. Validate with committee meeting, quorum, transfer, and parking queries.
